const ngrokLink = 'https://charge-api-oydrvkbt6q-uc.a.run.app';

const serverLinks = {
  outlookRedirectLink: `${ngrokLink}/auth/redirect`,
  cloudTasksRedirectLink: `${ngrokLink}/mails/send-mails`,
};

export default serverLinks;
