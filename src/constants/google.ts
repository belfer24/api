import serverLinks from "./localhostLinks";

const cloudTasksCreds = {
  project: 'charge-outlook-project',
  queue: 'outlook-queue',
  location: 'europe-central2',
  url: serverLinks.cloudTasksRedirectLink,
}

export default cloudTasksCreds;
